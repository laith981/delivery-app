import 'package:flutter/material.dart';

class ProfailPage extends StatelessWidget {
  const ProfailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child:Text("profail")
    );
  }
}